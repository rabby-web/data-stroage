const add = (fast, second) => {
    return fast + second;
}
// export default add;
const multiply = (fast, second) => {
    return fast * second;
}
// const sum = (fast, second) => {
//     return fast + second;
// }
export {add, multiply}